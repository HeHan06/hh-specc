# 澄清记录：ai-guide

> 阶段：clarify（澄清消歧）
> 结论：**无待澄清项**
> 完成判据：`spec.md` 中 `[NEEDS CLARIFICATION]` 标记数为 0

## 1. 扫描结论

已按 clarify 阶段指令逐项检查 `spec.md`，未发现会影响实现分支、需继续向用户提问的待澄清项。spec 正文中的业务决策已由前置探询与工程决策落定，全部关键分支均已明确。

## 2. 已锁定决策与影响

以下决策已在 `spec.md` 第 5 节记录，并已同步回填到功能需求与量化约束，澄清阶段不再重复提问。

| 决策项 | 决策内容 | 对实现的影响 |
|---|---|---|
| 打赏金额 | 固定枚举 0.1 / 1 / 5 / 10 / 50 / 100 元 | 前端仅展示合法档位；后端对枚举做白名单校验，非法金额拒绝 |
| 付费咨询计价 | 500 元/半小时；全平台前 10 个「经管理员确认排期」的咨询订单免费 | 管理员确认排期时占用免费名额；取消已确认免费订单释放名额 |
| 点赞重复点击 | 仅忽略，不取消、不重复计数 | 后端按「访客标识 + 内容标识」幂等去重，不做取消点赞 |
| 收款方式 | 不接入在线支付，打赏与付费咨询统一「加管理员微信」线下收款 | 留资后展示管理员微信号，管理员后台确认收款/排期；留言按隐私保密处理 |
| 内容「下架」语义 | 独立「已下架」状态，可恢复发布或归档，不退回草稿 | 内容状态机加入「已下架」中间态与「恢复发布」动作 |
| 分页与搜索词上限 | 分页默认 20、上限 100；搜索词长度上限 50 字符 | 接口入参校验固定为 20/100/50，超限返回统一校验错误 |

## 3. 检查维度

- 角色与权限：访客匿名可用范围、付费咨询必填联系方式、单一管理员账号均已锁定。
- 状态机：内容、打赏订单、咨询订单的合法流转与异常分支均已明确。
- 量化约束：分页、搜索词、金额、计价、性能指标均已给出具体数字。
- 端归属：每条 Req 均已标注阅读站/管理后台/双端，边界与范围清单完整。
- 异常路径：空数据、非法输入、权限不足、非法状态流转、并发冲突均已覆盖。
- 范围边界：订阅、内推、社区/评论、UGC、收藏、AI 能力等「不做」项明确，未发现范围外功能。

## 4. 留待 plan 阶段的技术决策

以下事项属于技术实现选型，不构成业务歧义，不在 clarify 阶段追问，由 plan 阶段根据宪法与平台知识细化。

- 无（支付已确定为「加管理员微信」线下收款，不涉及在线支付渠道选型）。
- 搜索实现方案（PostgreSQL 全文检索或应用层检索）及兜底查询策略。
- 推荐内容的排序/筛选实现细节。
- 联系方式、手机/微信的具体校验正则与错误提示。
- 匿名访客标识的生成与存储方案。
- 管理员初始账号与凭据注入方式。
