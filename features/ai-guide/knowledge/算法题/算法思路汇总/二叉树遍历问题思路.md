拿到题目
   |
   v
[第0步] 画图理解物理规则（向下？任意？拐弯？）
   |
   v
[第1步] 子结果能 O(1) 复用吗？| “如果我知道了左右子树返回的结果 R，我能否在 O(1) 时间内算出当前节点的答案，而不需要重新遍历左子树？”
   |                    |
  能（单递归O(n)）     不能（双重递归O(n²)）
   |                    |
   v                    v
[第2步] 数据流向？   [外层遍历所有节点] + [内层套三步法]
   |
   +-- 父传子？ --> 前序（参数传递）
   +-- 子传父？ --> 后序（返回值汇总）
   +-- 左中右？ --> 中序（BST专用）
   |
   v
[第3步] 套万能三步法（填参数、返回值、当前逻辑）
   |
   +-- 特别注意：当前逻辑中，左右子树能不能相加？
   |           （能=直径类，不能=向下路径类）
   |
   v
[第4步] 检查溢出 & 空指针
   |
   v
   提交 AC！✅