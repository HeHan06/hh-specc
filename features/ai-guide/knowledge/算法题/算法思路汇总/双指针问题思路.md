┌─────────────────────────────────────────────────┐
│  第1步：确定当前状态由哪两个变量决定              │
│  例：水量 = (right-left) × min(h[left], h[right])│
├─────────────────────────────────────────────────┤
│  第2步：假设移动左指针，分析结果会如何变化        │
│  是变大、变小、还是不确定？                      │
├─────────────────────────────────────────────────┤
│  第3步：假设移动右指针，分析结果会如何变化        │
│  同样分析变化趋势                               │
├─────────────────────────────────────────────────┤
│  第4步：找到一个指针，移动它后结果必然变差 → 
│  说明另一个指针没有潜力了，所以我们应该移动另一个指针 →  │
│    如果单独移动两个指针必然变差（即相等时）→ 考虑同时移动指针 │
└─────────────────────────────────────────────────┘
