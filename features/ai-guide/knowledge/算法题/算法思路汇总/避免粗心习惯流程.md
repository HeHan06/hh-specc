┌─────────────────────────────────────────────────────────────┐
│  第0步：读题三问（写代码之前，能画图先画图，测试驱动）->  │
│        “避免粗心”不是靠“更小心”，而是靠“释放脑内存”           │
│  □ 这道题在问什么？（用自己的话复述一遍）                  │
│  □ 边界条件有哪些？（空输入、单元素、全相同、极端大/小）   │
│  □ 预期输入输出是什么？（手写 2-3 个示例）                │
├─────────────────────────────────────────────────────────────┤
│  第1步：确定变量语义（谁是谁）                             │
│  □ 每个变量代表什么？                                      │
│  □ 循环变量指向“当前”还是“下一个”？                      │
│  □ 返回值代表什么？                                        │
├─────────────────────────────────────────────────────────────┤
│  第2步：写出“状态快照”（关键节点）                        │
│  □ 初始状态是什么？                                        │
│  □ 循环体执行一次后的状态是什么？                          │
│  □ 循环结束时的状态是什么？                                │
├─────────────────────────────────────────────────────────────┤
│  第3步：分层编码（先骨架后血肉）                           │
│  □ 先写边界处理（return 语句）                             │
│  □ 再写核心逻辑框架（循环/递归结构）                       │
│  □ 最后填充细节逻辑                                        │
├─────────────────────────────────────────────────────────────┤
│  第4步：提交前“慢读自检”                                   │
│  □ 用极端值走一遍逻辑（n=0, n=1, 最大值）                 │
│  □ 检查所有数组/字符串访问是否越界                        │
│  □ 检查所有循环/递归终止条件是否正确                       │
│  □ 检查返回值是否符合题目要求                              │
└─────────────────────────────────────────────────────────────┘