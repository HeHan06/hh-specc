<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiguide.content.mapper.TopicMapper">
    <resultMap id="TopicViewMap" type="com.aiguide.content.dto.TopicView">
        <constructor>
            <arg column="code" javaType="string"/>
            <arg column="name" javaType="string"/>
            <arg column="description" javaType="string"/>
            <arg column="sort_order" javaType="java.lang.Integer"/>
        </constructor>
    </resultMap>

    <select id="selectVisibleTopics" resultMap="TopicViewMap">
        SELECT code,
               name,
               description,
               sort_order
        FROM topic
        WHERE enabled = TRUE
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="isVisibleByCode" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM topic WHERE code = #{code} AND enabled = TRUE)
    </select>

    <resultMap id="AdminTopicViewMap" type="com.aiguide.admin.dto.AdminViews$TopicView">
        <constructor>
            <arg column="code" javaType="string"/>
            <arg column="name" javaType="string"/>
            <arg column="description" javaType="string"/>
            <arg column="sort_order" javaType="java.lang.Integer"/>
            <arg column="enabled" javaType="_boolean"/>
            <arg column="updated_at" javaType="string"/>
        </constructor>
    </resultMap>

    <sql id="AdminTopicWhere">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (code ILIKE CONCAT('%', #{keyword}, '%') OR name ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="enabled != null">
                AND enabled = #{enabled}
            </if>
        </where>
    </sql>

    <select id="selectAdminTopics" resultMap="AdminTopicViewMap">
        SELECT code,
               name,
               description,
               sort_order,
               enabled,
               TO_CHAR(updated_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') AS updated_at
        FROM topic
        <include refid="AdminTopicWhere"/>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAdminTopics" resultType="long">
        SELECT COUNT(*)
        FROM topic
        <include refid="AdminTopicWhere"/>
    </select>

    <insert id="insertTopic">
        INSERT INTO topic (code, name, description, sort_order, enabled)
        VALUES (#{code}, #{name}, #{description,jdbcType=VARCHAR}, #{sortOrder}, #{enabled})
    </insert>

    <update id="updateTopic">
        UPDATE topic
        SET name = #{name},
            description = #{description,jdbcType=VARCHAR},
            sort_order = #{sortOrder},
            enabled = #{enabled}
        WHERE code = #{code}
    </update>

    <select id="existsByCode" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM topic WHERE code = #{code})
    </select>
</mapper>
