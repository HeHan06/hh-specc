# hh-specc 框架源码开发规范（FRAMEWORK-DEV）

> **给谁看**：所有开发 / 修改 hh-specc 框架源码的 AI 或人。
> **适用范围**：框架本体源码 —— `lib/`、`specc.sh`、`.specc/`、`docs/`、`specc-observability/`、`.gitignore` 等平台组件。
> **不适用**：`features/<需求ID>/` 与 `projects/<需求ID>/` —— 那是「使用本框架」产出的业务代码，其规则由宪法约束。
> **如何使用**：本文件已通过仓库根目录的 `AGENTS.md`（Codex 约定入口）被「进入仓库的 AI」自动读取。修改框架源码前，读完 `AGENTS.md` 后应继续读本文件详细条款；读完仍须遵守本文件全部条款。

## 0. 一句话原则

**框架源码也要不重复造轮子：任何规范、契约、知识、规则，全仓库只允许存在一份源文件；其它任何地方对它一律「引用相对路径」，禁止复制粘贴其内容。** （与宪法 7.6 一致；宪法约束「生成的业务代码」，本文件约束「框架源码本身」。）

## 1. 单一真相源（DRY）

- **1.1 唯一源**：同一主题的规范/契约/平台知识/常量，必须收敛到一处定义（一个文件），不因「文件分散、阶段不同、端不同」而复制多份。
- **1.2 引用不复制**：其它文档需要引用某条规则时，用「相对路径 + 条款号」指向源文件，**禁止把源内容粘贴过来**。被引用文件必须真实存在。
- **1.3 冲突裁决**：引用方与源文件冲突时，一律以源文件为准，并立即修订引用方的错误转述（若引用方只是在转述而非引用，则视为违规，必须改为引用）。
- **1.4 上下游一致性**：修改源文件后，须同步核查所有指向它的引用方是否仍然成立（路径、条款号、名称未漂移）。

## 2. 不重复造轮子的常见场景

- **平台层知识**：`.specc/platform/*.md` 按主题一文件（如 `api-conventions`、`frontend-architecture`、`web-admin-standards`、`miniprogram-standards`、`dual-end-boundary`）。新增主题新建文件；不得把某主题内容塞进多个文件，也不得在 `tech-stack.md` 里复述其它文件的要点。
- **阶段指令**：`.specc/prompts/*.md` 只定义「本阶段要做什么、输出什么」，需引用平台知识时写引用，不复制要点。
- **模板**：`.specc/templates/*.md` 是产物骨架，与平台规范各司其职，不重复。
- **配置与常量**：版本号、路径、默认值等收敛到 `.specc/config.yaml` 或 `lib/common.sh`，禁止在多个脚本/文档里硬编码同一常量。
- **脚本逻辑**：`lib/*.sh` 中重复出现的公共函数应抽取到 `common.sh`，禁止跨脚本复制同段逻辑。

## 3. 维护准则

- **3.1 改动最小化**：只改与被解决需求直接相关的文件，不顺手重构无关代码。
- **3.2 先引用后新建**：需要一条规则时，先确认仓库里是否已有源文件；有则引用，无才新建。禁止「因为不知道有没有就再写一份」。
- **3.3 新增即声明**：新建任何源文件/主题时，在文档内注明它与已有文件的边界，避免与既有主题重叠。

## 4. 自检清单（改框架源码后逐条打钩）

1. 我新增的规则是否已存在源文件？若存在，我改成引用了吗？（而非复制）
2. 我是否在非源文件处粘贴了源内容？若有 → 改为引用。
3. 我修改的源文件，其所有引用方是否仍成立（路径/条款号/名称）？
4. 是否有同一常量/路径/版本在多个文件里被重复硬编码？
5. 是否只改了本需求相关文件，没有顺手做无关重构？

## 5. 与宪法的关系（避免套用错层）

| 层次 | 约束对象 | 强制注入机制 | 所在文件 |
|---|---|---|---|
| 宪法 | **使用本框架生成的业务代码** | M5 组装器所有阶段强制注入 | `.specc/constitution.md` |
| 本文件 | **本框架源码本身** | 人工约定：改框架源码前先读本文件 | 本文件（仓库根目录） |

两者规则内核相通（单一真相源/不重复造轮子），但受众与生效路径不同，不得互相替代。生成业务代码的 AI 读宪法；开发框架源码的 AI 读本文件。
