# AGENTS.md — hh-specc 框架源码开发自动入口

> **Codex 约定**：本文件是 Codex 在仓库内执行任务时**自动读取**的仓库级说明（AGENTS 约定）。
> 进入本仓库的 AI / 代理在**任何操作前**都会先加载本文件，因此这里是「框架源码开发规范」的自动化入口。
> **目标**：让「开发/修改 hh-specc 框架源码」的 AI 无需人工提醒，每次都能感知到「单一真相源、不重复造轮子」的规则。

## 这是什么仓库

`hh-specc` —— 一个从自然语言需求到合规代码的 AI Coding 框架（融合 Spec Kit / Kiro / BMAD / OpenSpec 风格）。它通过六阶段（specify / clarify / plan / tasks / implement / verify）驱动一个引擎生成业务代码。

## 你正在修改哪一类代码？（必须先确认再动手）

在本仓库操作前，先判断你要改的代码归属哪一层，因为它们**遵守不同的规则**，不要套错：

| 你改的对象 | 例子 | 必须遵守的规范 |
|---|---|---|
| **框架源码** | `lib/`、`specc.sh`、`.specc/`、`docs/`、`specc-observability/` | **本仓库的开发规范**（见下） |
| 平台层知识 | `.specc/platform/*.md` | 本仓库开发规范 + 一主题一文件 |
| **业务代码**（本框架产出的） | `features/<需求ID>/`、`projects/<需求ID>/` | 由宪法 `.specc/constitution.md` 约束（M5 组装器注入） |

## 框架源码开发规范（必读）

> 完整条款见 **[`FRAMEWORK-DEV.md`](./FRAMEWORK-DEV.md)**。以下为自动化入口的核心摘要。

**总原则：框架源码也要不重复造轮子 —— 任何规范、契约、知识、规则，全仓库只允许存在一份源文件；其它任何地方对它一律「引用相对路径」，禁止复制粘贴其内容。**

四条铁律：
1. **唯一源**：同一主题的规范/契约/平台知识/常量收敛到一处定义，不因文件分散、阶段不同、端不同而复制多份。
2. **引用不复制**：其它文档引用某条规则时，用「相对路径 + 条款号」指向源文件，禁止粘贴源内容；被引用文件必须真实存在。
3. **冲突裁决**：引用方与源文件冲突时以源文件为准，并立即修订引用方错误的转述。
4. **上下游一致性**：改源文件后同步核查所有引用方仍成立（路径/条款号/名称未漂移）。

自检清单：改框架源码后逐条打钩（详见 `FRAMEWORK-DEV.md` §4）——新增规则是否已有源文件？是否在非源文件处粘贴了源内容？源文件的引用方是否仍成立？是否重复硬编码同一常量/路径/版本？是否只改了本需求相关文件？

## 违规会怎样

若在本次修改中违反了上述规则（例如把平台知识要点复制进多个文件、在 `tech-stack.md` 里复述其它文件内容、重复硬编码版本号），这类改动将被标记为违规并要求整改 —— 本仓库把「避免重复」视为一等规范，而非审美建议。

## 注意：不要把这文件当业务代码规范

`AGENTS.md` / `FRAMEWORK-DEV.md` 约束的是「**开发框架源码**」的 AI。若你在生成 `features/`、`projects/` 下的业务代码，请忽略本文件，转而遵守 `.specc/constitution.md`（由框架的 M5 组装器自动注入，无需你手动读）。
